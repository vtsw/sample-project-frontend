import { Meta, Story, Preview,Props } from '@storybook/addon-docs/blocks';
import WelcomeDialog from '../../src/views/User/components/WelcomeDialog/WelcomeDialog'

import * as stories from './WelcomeDialog.stories.js';

<Meta title="Component Api|WelcomeDialog" />

# WelcomeDialog
With `MDX` we can define a story for `Welcome Dialog` right in the middle of our
markdown documentation.




  <Story name="Basic">
    {stories.basic()}
  </Story>


```jsx 
	import { Welcomedialog } from '@views_components'

	...
	const [dialogVisible, setDialogVisible] = useState(true)

	...

	const handleSetDialogVisible = () => {
		setDialogVisible(false)
	}

	return (
		...
		{dialogVisible ? (
			<WelcomeDialog setDialogVisible={handleSetDialogVisible} />
		) : null}
		...
	)
```

# PropTypes

<Props of={WelcomeDialog}/>

