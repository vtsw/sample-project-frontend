import { Meta, Story, Preview,Props } from '@storybook/addon-docs/blocks';
import LoadingScreen from './LoadingScreen'

import * as stories from './LoadingScreen.stories.js';

<Meta title="Component Api|LoadingScreen" />

# LoadingScreen
With `MDX` we can define a story for `Loading Screen` right in the middle of our
markdown documentation.




  <Story name="Basic">
    {stories.basic()}
  </Story>


```jsx 
  import { Loading } from '@views_components'

	...
	const { loading, error, data, fetchMore, networkStatus } = useQuery(
	...

	return (
		<Box> // position: 'relative'
			...
			<Loading
				open={loading && networkStatus !== NETWORK_STATUS_FETCH_MORE}
				msg={'Loading...'}
			/>
			...
		</Box>
	)
```

# PropTypes

<Props of={LoadingScreen}/>

